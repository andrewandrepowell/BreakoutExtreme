namespace BreakoutExtreme.Components
{
    public partial class Animater
    {
        public enum Atlases
        {
            Ball,
            Paddle,
            BrickLarge
        }
        public enum Animations
        {
            Ball,
            Paddle,
            BrickLarge
        }
        public enum Layers
        {
            Shadow,
            Ground
        }
    }
}
